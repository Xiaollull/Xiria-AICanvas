accelerate>=1.2,<2
diffusers>=0.38,<0.39
fastapi>=0.115,<1
huggingface-hub>=0.34,<1
numpy>=2,<3
onnxruntime>=1.20,<2
pillow>=11,<13
peft>=0.17,<1
psutil>=6,<8
pydantic>=2.10,<3
# FLUX.2 [dev]'s Mistral tokenizer is a tekken table whose split pattern uses Unicode property
# classes the standard library's `re` cannot compile. It arrives with transformers today; it is
# declared here because `tekken_tokenizer` imports it directly.
regex>=2024.5
safetensors>=0.5,<1
scipy>=1.14,<2
spandrel==0.4.2
spandrel-extra-arches==0.2.0
transformers>=4.51,<5
ultralytics>=8.3,<9
uvicorn>=0.34,<1
